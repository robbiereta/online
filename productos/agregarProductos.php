<?php
 $arrayProds = array();
foreach($_POST as $cantidad => $can){
    $canFinal = "\$" . $cantidad . "='" . $can . "';";
   
    // echo  "<span>Cantidad :".$can . "</h3>";
    // $checkCan=is_string($can);

    // if($checkCan){
    //     echo"es string";
    // }
       
      var_dump($can);
     $arrayProds ;

 
}
for ($i=0;  $i++;) { 
    $post2=$_POST;
    var_dump($post2);
}
?>
